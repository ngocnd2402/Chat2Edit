import ChatPage from "@/pages/ChatPage";

const Chat = (): JSX.Element => {
    return (
        <main className="h-screen overflow-hidden">
            <ChatPage />
        </main>
    );
}

export default Chat;